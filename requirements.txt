# meetai-ai-workers — merged from AudioWorker + gateway/worker deps
torch>=2.6.0
torchaudio>=2.6.0
numpy>=1.24,<2  # torch/whisper wheels on Windows still expect NumPy 1.x ABI
av>=12.0                    # PyAV: WebM/Opus decode (bundles FFmpeg libs, no system install)

openai-whisper
pyannote.audio>=4.0
protobuf>=4.25.3,<5
speechbrain==1.0.3

transformers>=4.38,<5
accelerate>=0.27
sentencepiece>=0.1.99
huggingface-hub>=1.0

# Newer mediapipe wheels (e.g. 0.10.35) omit ``mp.solutions``; Face Mesh needs a build that still ships it.
mediapipe==0.10.14
opencv-python>=4.9

redis>=5.0
pydantic>=2.6
python-dotenv>=1.0
fastapi>=0.109
uvicorn[standard]>=0.27
python-multipart>=0.0.9
